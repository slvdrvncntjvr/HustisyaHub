{
  "name": "hustisyahub",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --hot index.ts",
    "start": "bun run index.ts",
    "init": "bun run generate.ts --init",
    "generate": "bun run generate.ts",
    "generate:component": "bun run generate.ts --type component",
    "generate:hook": "bun run generate.ts --type hook",
    "generate:util": "bun run generate.ts --type util",
    "test": "bun test"
  },
  "dependencies": {
    "lucide-react": "^0.555.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.2.45",
    "@types/react-dom": "^18.2.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
